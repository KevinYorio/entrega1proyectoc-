using SistemaGestionData;
using SistemaGestionEntities;

namespace SistemaGestionBusiness
{
    public static class ProductoBusiness
    {
        public static void Metodo1()
        {
            // Lógica relacionada con productos
        }

        // Otros métodos relacionados con productos

        public static void CrearProducto(Producto producto)
        {
            ProductoData.CrearProducto(producto);
        }

        // Otros métodos relacionados con productos
    }

    public static class UsuarioBusiness
    {
        public static void Metodo1()
        {
            // Lógica relacionada con usuarios
        }

        // Otros métodos relacionados con usuarios

        public static void CrearUsuario(Usuario usuario)
        {
            UsuarioData.CrearUsuario(usuario);
        }

        // Otros métodos relacionados con usuarios
    }

    public static class VentaBusiness
    {
        public static void Metodo1()
        {
            // Lógica relacionada con ventas
        }

        // Otros métodos relacionados con ventas
    }

    public static class ProductoVendidoBusiness
    {
        public static void Metodo1()
        {
            // Lógica relacionada con productos vendidos
        }

        // Otros métodos relacionados con productos vendidos
    }
}
